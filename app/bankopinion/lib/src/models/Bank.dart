class Bank {

  int id;
  String address;
  String shortName;
  var bankInfo = 
  {
    'shortName' : ""
  };

  // Constructor, with syntactic sugar for assignment to members.
  Bank(this.id, this.address, this.shortName, this.bankInfo);
}

void main(){

}